require "spec_helper"

describe Paystub::Sinatra do
  it "has a version number" do
    expect(Paystub::Sinatra::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
